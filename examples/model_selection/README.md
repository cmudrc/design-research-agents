## Model Selection Examples

Run from repo root:

```bash
PYTHONPATH=src python3 examples/model_selection/local.py
PYTHONPATH=src python3 examples/model_selection/remote.py
```

Notes:
- `local.py` demonstrates a tight-cost configuration that stays local.
- `remote.py` demonstrates constraints that allow/prefer remote capability.
